import 'package:flutter/material.dart';

class ReviewPostingSubscreen extends StatefulWidget {
  const ReviewPostingSubscreen({Key? key}) : super(key: key);

  @override
  State<ReviewPostingSubscreen> createState() => _ReviewPostingSubscreenState();
}

class _ReviewPostingSubscreenState extends State<ReviewPostingSubscreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
