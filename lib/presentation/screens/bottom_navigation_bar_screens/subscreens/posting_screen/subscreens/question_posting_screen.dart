import 'package:flutter/material.dart';

class QuestionPostingSubscreen extends StatefulWidget {
  const QuestionPostingSubscreen({Key? key}) : super(key: key);

  @override
  State<QuestionPostingSubscreen> createState() =>
      _QuestionPostingSubscreenState();
}

class _QuestionPostingSubscreenState extends State<QuestionPostingSubscreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
