import 'package:flutter/material.dart';

class SpecsComparisonTable extends StatelessWidget {
  const SpecsComparisonTable({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Table(
      children: <TableRow>[],
    );
  }
}
